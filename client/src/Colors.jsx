import React from "react";

const Colors = (props) => {
  return (
    <div>
      {
        props.data.map((item, i) => {
          return (

          )
        })
      }
    </div>
  )
}




export default Colors;

